from setuptools import setup

setup(
    name="combustive",
    version="0.0.1",
    description="A set of utilities for the python multiprocessing and threading modules",
    packages=["combustive"],
)